[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "requestguard"
version = "0.2.0"
description = "Framework-agnostic Python rate limiting with six algorithms, sync and async support, and optional Redis storage."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Subhan Adeel" },
]
maintainers = [
    { name = "Subhan Adeel" },
]

keywords = [
    "rate-limiting",
    "rate-limiter",
    "request-limiter",
    "requestguard",
    "python",
    "api",
    "throttling",
    "traffic-control",
    "middleware",
    "decorator",
    "token-bucket",
    "leaky-bucket",
    "fixed-window",
    "distributed-rate-limiting",
    "in-memory",
    "redis",
    "backend",
    "cache",
    "web",
    "http",
    "rest-api",
    "microservices",
    "framework-agnostic",
    "concurrency",
    "api-security",
    "dos-protection",
    "abuse-prevention",
    "request-throttling",
    "traffic-shaping",
    "quota",
    "burst-control",
    "networking",
    "performance",
    "scalability",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://github.com/AdeelMalik22/rateguard"
Maintainer = "https://github.com/AdeelMalik22"
LinkedIn = "https://www.linkedin.com/in/subhan-adeel-4b6b14326/"
Documentation = "https://github.com/AdeelMalik22/rateguard#readme"
Repository = "https://github.com/AdeelMalik22/rateguard"
Issues = "https://github.com/AdeelMalik22/rateguard/issues"
Changelog = "https://github.com/AdeelMalik22/rateguard/blob/master/CHANGELOG.md"


[project.optional-dependencies]
redis = ["redis>=5.0"]
dev = [
    "redis>=5.0",
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "httpx>=0.24.0",
    "uvicorn>=0.20.0",
    "build>=1.0.0",
    "twine>=4.0.0",
]


[tool.setuptools.packages.find]
where = ["."]
include = ["requestguard*"]

[tool.pytest.ini_options]
markers = [
    "integration: requires external services such as Redis",
]


[tool.setuptools.package-data]
requestguard = ["py.typed"]
